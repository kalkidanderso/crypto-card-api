export enum CardStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  BLOCKED = 'blocked',
  FROZEN = 'frozen',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired',
}

export enum CardType {
  VIRTUAL = 'virtual',
  PHYSICAL = 'physical',
}
